import React from 'react';
import MainLayout from '../../components/common/main-layout/MainLayout';
import { Stack, Typography } from '@mui/material';

const PhonesPage = () => {
	return (
		<MainLayout>
			<Stack>
				<Typography sx={{ color: 'black' }}>Phonespage</Typography>
			</Stack>
		</MainLayout>
	);
};

export default PhonesPage;

