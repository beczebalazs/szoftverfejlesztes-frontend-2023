import React from 'react';
import MainLayout from '../../components/common/main-layout/MainLayout';
import { Stack, Typography } from '@mui/material';

const GamingPage = () => {
	return (
		<MainLayout>
			<Stack>
				<Typography sx={{ color: 'black' }}>Gamingpage</Typography>
			</Stack>
		</MainLayout>
	);
};

export default GamingPage;

